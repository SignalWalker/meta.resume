{
  "scripts": {
    "to-pdf": "npx resumed validate && npx resumed render -o _site/index.html && node pdf.js",
    "build": "npx resumed render resume.json"
  },
  "dependencies": {
    "jsonresume-theme-stackoverflow": "github:phoinixi/jsonresume-theme-stackoverflow",
    "puppeteer": "^20.7.3",
    "resumed": "^3.0.1"
  }
}
